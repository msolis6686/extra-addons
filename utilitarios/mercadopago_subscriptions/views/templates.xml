<?xml version="1.0" ?>
<odoo>

    <!-- Mail template are declared in a NOUPDATE block
         so users can freely customize/delete them -->
    <!-- <data noupdate="1"> -->
    <data>
        <!--Email template -->
        <record id="email_template_mercadopago_subscription" model="mail.template">
            <field name="name">Subscripcion MercadoPago - Send by Email</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="email_from">${(user.email_formatted) |safe}</field>
            <field name="partner_to">${object.id}</field>
            <field name="subject">${user.company_id.name} Subscripcion ${object.subscription_type.name}</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        Estimado: ${object.name}<br/>

        Gracias por sumarte como socio del Cluster Tecnologico Salta ¡bienvenido!<br/>

        Te invitamos a que ingreses en el siguiente link para registrar el pago de la cuota.<br/>
        <br/>
        <a style="margin-left: 120px;" href="${object.subscription_type.mp_link}">
            <img class="oe_edi_paypal_button" src="/mercadopago_subscriptions/static/src/img/boton_mp_subscribirse.png"/>
        </a>
        <br/>
        <br/>
        En caso de no poder hacer click en el boton, puede hacer click <a href="${object.subscription_type.mp_link}">AQUI</a>
        o tambien puede copiar y pegar el link proporcionado mas abajo en su navegador.<br/>
        ${object.subscription_type.mp_link}
        <br/>
        <br/>
        Estamos a disposición ante cualquier duda o sugerencia.<br/>

        Saludos!<br/>
    </p>
</div>
            </field>
            <!-- <field name="report_template" ref="account.account_invoices"/> -->
            <field name="report_name">Subscripcion de ${object.name}</field>
            <field name="lang">${object.lang}</field>
            <field name="user_signature" eval="False"/>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>
